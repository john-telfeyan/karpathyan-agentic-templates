# Testing

> Stub. Linked from Operating Rule **"New logic gets tests."** Fill in as the
> suite takes shape.

## How to run

- `<command>` — run the full test suite.
- `<command>` — run a single test / watch mode.

## Conventions

- Unit-test logic as features land. Don't build test infrastructure for its own sake.
- Where tests live: `<tests/ layout>`.
- Naming / fixtures: `<convention>`.

## Coverage notes

- What's covered: `<…>`
- Known gaps: `<…>`
